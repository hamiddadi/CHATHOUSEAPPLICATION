diff --git a/node_modules/image-size/dist/types/icns.js b/node_modules/image-size/dist/types/icns.js
index f2bfafe..c7724ed 100644
--- a/node_modules/image-size/dist/types/icns.js
+++ b/node_modules/image-size/dist/types/icns.js
@@ -12,6 +12,7 @@ const utils_1 = require("./utils");
  */
 const SIZE_HEADER = 4 + 4; // 8
 const FILE_LENGTH_OFFSET = 4; // MSB => BIG ENDIAN
+const ENTRY_HEADER_SIZE = 4 + 4; // 8
 /**
  * Image Entry
  *
@@ -63,12 +64,17 @@ const ICON_TYPE_SIZE = {
     // . => 1024 x 1024
     ic10: 1024,
 };
-function readImageHeader(input, imageOffset) {
+function readImageHeader(input, imageOffset, fileLength) {
+    const remainingLength = Math.min(input.length, fileLength) - imageOffset;
+    if (remainingLength < ENTRY_HEADER_SIZE) {
+        throw new TypeError('Invalid ICNS entry header');
+    }
     const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
-    return [
-        (0, utils_1.toUTF8String)(input, imageOffset, imageLengthOffset),
-        (0, utils_1.readUInt32BE)(input, imageLengthOffset),
-    ];
+    const imageLength = (0, utils_1.readUInt32BE)(input, imageLengthOffset);
+    if (imageLength < ENTRY_HEADER_SIZE || imageLength > remainingLength) {
+        throw new TypeError('Invalid ICNS entry length');
+    }
+    return [(0, utils_1.toUTF8String)(input, imageOffset, imageLengthOffset), imageLength];
 }
 function getImageSize(type) {
     const size = ICON_TYPE_SIZE[type];
@@ -79,8 +85,14 @@ exports.ICNS = {
     calculate(input) {
         const inputLength = input.length;
         const fileLength = (0, utils_1.readUInt32BE)(input, FILE_LENGTH_OFFSET);
+        if (inputLength < SIZE_HEADER ||
+            !Number.isSafeInteger(fileLength) ||
+            fileLength < SIZE_HEADER + ENTRY_HEADER_SIZE ||
+            fileLength > inputLength) {
+            throw new TypeError('Invalid ICNS file length');
+        }
         let imageOffset = SIZE_HEADER;
-        let imageHeader = readImageHeader(input, imageOffset);
+        let imageHeader = readImageHeader(input, imageOffset, fileLength);
         let imageSize = getImageSize(imageHeader[0]);
         imageOffset += imageHeader[1];
         if (imageOffset === fileLength)
@@ -91,7 +103,7 @@ exports.ICNS = {
             width: imageSize.width,
         };
         while (imageOffset < fileLength && imageOffset < inputLength) {
-            imageHeader = readImageHeader(input, imageOffset);
+            imageHeader = readImageHeader(input, imageOffset, fileLength);
             imageSize = getImageSize(imageHeader[0]);
             imageOffset += imageHeader[1];
             result.images.push(imageSize);
diff --git a/node_modules/image-size/dist/types/utils.js b/node_modules/image-size/dist/types/utils.js
index 5224bba..9a6957b 100644
--- a/node_modules/image-size/dist/types/utils.js
+++ b/node_modules/image-size/dist/types/utils.js
@@ -49,10 +49,13 @@ function readUInt(input, bits, offset, isBigEndian) {
 }
 exports.readUInt = readUInt;
 function readBox(input, offset) {
-    if (input.length - offset < 4)
+    // ISO BMFF boxes need at least an eight-byte size/name header. Reject
+    // zero, extended-size and truncated boxes: this parser does not implement
+    // those encodings, and returning them would let callers stop advancing.
+    if (input.length - offset < 8)
         return;
     const boxSize = (0, exports.readUInt32BE)(input, offset);
-    if (input.length - offset < boxSize)
+    if (boxSize < 8 || input.length - offset < boxSize)
         return;
     return {
         name: (0, exports.toUTF8String)(input, 4 + offset, 8 + offset),
@@ -67,9 +70,10 @@ function findBox(input, boxName, offset) {
             break;
         if (box.name === boxName)
             return box;
-        // Fix the infinite loop by ensuring offset always increases
-        // If box.size is 0, advance by at least 8 bytes (the size of the box header)
-        offset += box.size > 0 ? box.size : 8;
+        const nextOffset = box.offset + box.size;
+        if (nextOffset <= offset)
+            break;
+        offset = nextOffset;
     }
 }
 exports.findBox = findBox;
