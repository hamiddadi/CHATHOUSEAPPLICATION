{
  "//": "React Native app — the Android build runs via ./gradlew / the react-native CLI, NOT the VSCode Java language server. We run that server in LightWeight mode so it never imports or builds the Gradle plugins under node_modules (@react-native/gradle-plugin, expo-*-gradle-plugin, @livekit/*-expo-plugin). In Standard/Hybrid mode it imported them and reported phantom 'Missing Gradle project configuration folder: .settings' / 'missing required library …shared-testutil.jar' / 'project cannot be built' errors that NEVER affected the real Gradle build. There is no Java application code in this repo (native code is Kotlin, handled by a separate extension), so LightWeight loses nothing here. Revert java.server.launchMode to 'Hybrid' if you ever need full Java project features.",
  "//2": "SEPARATE, HARMLESS NOISE (Problems panel source:'Gradle', not 'Java'): the Microsoft 'Gradle for Java' extension (vscjava.vscode-gradle) bundles a Gradle Language Server that parses each library build.gradle under node_modules IN ISOLATION and flags 'unable to resolve class …' — GradleException (@livekit/react-native), com.android.Version + io.invertase.gradle.common.PackageJson + org.gradle.internal.jvm.Jvm (@react-native-firebase/app), groovy.json.JsonSlurper + com.android.build.gradle.tasks.ExternalNativeBuildJsonTask + org.apache.tools.ant.taskdefs.condition.Os (react-native-reanimated, react-native-image-picker). All those classes exist at REAL build time (Android Gradle Plugin / Groovy / plugin classpath) but not when the LS parses a script standalone → 100% false positives; ./gradlew assembleDebug is unaffected. We do not use that extension (Android builds run from the CLI, not the Gradle Tasks panel). FIX: Extensions view → search '@installed gradle' → 'Gradle for Java' → gear → Disable (Workspace), then Reload Window. It's listed in .vscode/extensions.json unwantedRecommendations. If no such extension is installed, the diagnostics are stale from a removed extension — Reload Window clears them.",
  "java.server.launchMode": "LightWeight",
  "java.configuration.updateBuildConfiguration": "disabled",
  "java.compile.nullAnalysis.mode": "automatic",
  "java.import.exclusions": ["**/node_modules/**", "**/.git/**", "**/build/**"],
  "java.import.gradle.enabled": false,
  "java.import.maven.enabled": false,
  "gradle.nestedProjects": false
}
