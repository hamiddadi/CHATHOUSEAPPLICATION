<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
  <!-- Release and every non-debug variant are TLS-only. Debug source sets
       replace this resource when Metro/local development needs HTTP. -->
  <base-config cleartextTrafficPermitted="false" />
</network-security-config>
